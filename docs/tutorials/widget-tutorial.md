# Widget tutorial

**Placeholder** — This tutorial will walk through building a simple user interface using the Glyph widget system.

## Planned content

- Introduction to the widget system architecture.
- Creating a canvas and enabling widget mode.
- Adding a label widget.
- Adding a button widget and handling click events.
- Adding a progress bar widget and updating it dynamically.
- Arranging widgets in a container (horizontal and vertical layout).
- Widget styling (colors, borders, backgrounds).
- Handling input events (buttons, touch).
- Complete example: a simple sensor dashboard with live updates.
